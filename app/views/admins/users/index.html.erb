<p id="notice"><%= notice %></p>

<h1>Admins Users</h1>

<table class="table">
  <thead>
    <tr>
      <th>ニックネーム</th>
      <th>email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admins_users.each do |admins_user| %>
      <tr>
        <td><%= admins_user.nickname %></td>
        <td><%= admins_user.email %></td>
        <td><%= link_to 'Show', admins_user_path(admins_user.id) %></td>
        <td><%= link_to 'Edit', edit_admins_user_path(admins_user.id) %></td>
        <td><%= link_to 'Destroy', admins_user_path(admins_user.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
